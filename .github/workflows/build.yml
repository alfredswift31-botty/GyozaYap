name: Build

on:
  push:
    branches: ["**"]
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Publish a release under this tag, e.g. v1.1. Leave empty to only build."
        required: false
        default: ""

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode 26
        run: |
          ls -d /Applications/Xcode*.app
          sudo xcode-select -s "$(ls -d /Applications/Xcode_26*.app | sort -V | tail -1)"
          xcodebuild -version

      # Ad-hoc signing ("-") so CI needs no certificates. Hardened runtime is
      # off for tests only: its library validation rejects the ad-hoc test
      # bundle injected into the ad-hoc host app.
      - name: Unit tests
        run: |
          xcodebuild test \
            -project GyozaYap.xcodeproj \
            -scheme GyozaYap \
            -only-testing:GyozaYapTests \
            -destination 'platform=macOS' \
            CODE_SIGN_IDENTITY=- CODE_SIGN_STYLE=Manual DEVELOPMENT_TEAM= \
            ENABLE_HARDENED_RUNTIME=NO

      - name: Build release app
        run: |
          xcodebuild build \
            -project GyozaYap.xcodeproj \
            -target GyozaYap \
            -configuration Release \
            SYMROOT="$PWD/build" \
            CODE_SIGN_IDENTITY=- CODE_SIGN_STYLE=Manual DEVELOPMENT_TEAM=

      # Facts the app depends on at runtime that no unit test can see.
      - name: Verify the built app
        run: |
          APP=build/Release/GyozaYap.app
          BIN="$APP/Contents/MacOS/GyozaYap"
          echo "== Privacy strings =="
          for key in NSMicrophoneUsageDescription NSSpeechRecognitionUsageDescription NSAudioCaptureUsageDescription; do
            /usr/libexec/PlistBuddy -c "Print :$key" "$APP/Contents/Info.plist" || { echo "Missing $key"; exit 1; }
          done
          echo "== Entitlements =="
          codesign -d --entitlements - --xml "$APP" > entitlements.plist
          for key in com.apple.security.app-sandbox com.apple.security.device.audio-input com.apple.security.network.client; do
            grep -q "$key" entitlements.plist || { echo "Missing entitlement $key"; exit 1; }
          done
          echo "== Linked libraries =="
          otool -L "$BIN"
          echo "== FoundationModels must be weak so the app launches on macOS 15 =="
          otool -l "$BIN" | grep -B2 -A2 "FoundationModels"
          otool -l "$BIN" | grep -A2 LC_LOAD_WEAK_DYLIB | grep -q FoundationModels \
            || { echo "FoundationModels is not weak-linked"; exit 1; }
          echo "== Minimum macOS =="
          /usr/libexec/PlistBuddy -c "Print :LSMinimumSystemVersion" "$APP/Contents/Info.plist"

      - name: Package
        run: |
          codesign --verify --deep --strict build/Release/GyozaYap.app
          ditto -c -k --keepParent build/Release/GyozaYap.app GyozaYap.zip

      - uses: actions/upload-artifact@v4
        with:
          name: GyozaYap
          path: GyozaYap.zip

  # Runs for a pushed v* tag, or a manual run given a release tag (GitHub
  # then creates the tag on the built commit).
  release:
    if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && inputs.release_tag != '')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: GyozaYap

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG_INPUT: ${{ inputs.release_tag }}
        run: |
          TAG="${TAG_INPUT:-$GITHUB_REF_NAME}"
          gh release create "$TAG" GyozaYap.zip \
            --repo "$GITHUB_REPOSITORY" \
            --target "$GITHUB_SHA" \
            --title "GyozaYap ${TAG#v}" \
            --generate-notes
